---
name: 作業
about: 何をやるかを1件書く
---

<!--
このコメントは消してよい。役割と時制は docs/README.md の「入れ物と役割」の表が定める。
文章のルールは .claude/rules/writing.md にある。

完了したときに何をデモできるかを、1文で言えること。言えないものは層（取得 / 型 /
コンポーネント）で切っている。1つの薄い経路を全層に通す形へ切り直す。

要件本文を再掲しない。ID で参照する。

  ✓ 要件: FR-99
    ## 完了条件
    - 過去の版が一覧できる
    - 任意の版に戻せる

  ✗ ## 概要
    記事の版管理ができるようにする。過去の版を保持し…
     → 要件文書のコピー。要件が変わると嘘になる

段取りは本文のチェックリストで持つ。sub-issue に分けるのは、1件では扱えないと
分かってからでよい。
-->

## 完了条件

<!-- 完了したときに何をデモできるか -->

## 依存

<!--
必ず書く。無ければ「なし」と書く。あるものは Issue 番号で示す。番号で示さないと、
どの作業が終われば着手できるのかが辿れない。

  ✓ なし。
  ✓ #72「D1 のスキーマを決める」。
  ✗ migrations/ が main にあること。 → どの Issue が持つか辿れない
-->
